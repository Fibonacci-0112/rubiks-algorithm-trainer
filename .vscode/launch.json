{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Web: Next.js dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm --filter @rubiks/web dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
